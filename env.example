# Environment template for Communication_LTD web app

# --- App ---
SESSION_SECRET=change-me
PORT=3000

# Data backend: mysql (production) or memory (dev)
DATA_BACKEND=mysql

# App mode: secure (default) or vuln (to demo XSS/SQLi)
MODE=secure

# --- MySQL ---
DB_HOST=localhost
DB_USER=root
DB_PASS=YOUR_MYSQL_PASSWORD
DB_NAME=comunicationltd

# --- Email (Gmail / App Password) ---
EMAIL_USER=YOUR_GMAIL_ADDRESS@gmail.com
EMAIL_PASS=YOUR_GMAIL_APP_PASSWORD
